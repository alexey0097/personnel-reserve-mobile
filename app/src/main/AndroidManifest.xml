<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="ru.iworking.personnel.reserve.mobile">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.personnel.reserve.mobile">
        <activity
                android:name=".activity.MainActivity"
                android:label="@string/app_name"
                android:theme="@style/Theme.personnel.reserve.mobile.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activity.ResumeEditActivity"
                  android:label="@string/activity_resume_edit"/>
        <activity android:name=".activity.ResumeCreateActivity"
                  android:label="@string/activity_resume_create"/>
        <activity android:name=".activity.ResumeViewActivity"
                  android:label="@string/activity_resume_view"/>
    </application>

</manifest>